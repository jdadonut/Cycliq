using Cycliq.Agents;

namespace Cycliq.Stores
{
    class GuildSettingsStore
    {
        private DatabaseAgent GuildSettingsDatabase;
        public GuildSettingsStore(DatabaseAgent GuildSettingsDatabase) 
        { 
            this.GuildSettingsDatabase = GuildSettingsDatabase;
        }
        public static void OnNewGuild(ulong guildid)
        {
            
        }
    }
}